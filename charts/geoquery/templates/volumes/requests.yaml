{{- $pvname := printf "%s-requests" .Release.Namespace }}
{{- $pvexists := lookup "v1" "PersistentVolume" "" $pvname }}
{{- if not $pvexists -}}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ $pvname }}
  annotations:
    pv.kubernetes.io/provisioned-by: nfs.csi.k8s.io
spec:
  capacity:
    # Not enforced for NFS — only used for PV/PVC binding (must be >= the PVC request)
    storage: {{ .Values.volumes.requests.size }}
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: {{ .Values.volumes.requests.storageClassName }}
  claimRef:
    namespace: {{ .Release.Namespace }}
    name: geoquery-requests
  mountOptions:
    {{- toYaml .Values.volumes.requests.mountOptions | nindent 4 }}
  csi:
    driver: nfs.csi.k8s.io
    # volumeHandle format: {nfs-server-address}#{share-name}#{sub-dir-name}#{uniquifier}
    # make sure this value is unique for every share in the cluster;
    # the namespace suffix keeps it unique across releases mounting the same share
    volumeHandle: {{ printf "%s#%s#%s#%s" .Values.volumes.requests.server .Values.volumes.requests.share .Values.volumes.requests.subDir .Release.Namespace }}
    volumeAttributes:
      server: {{ .Values.volumes.requests.server }}
      share: {{ .Values.volumes.requests.share }}
      {{- with .Values.volumes.requests.subDir }}
      subDir: {{ . }}
      {{- end }}
{{- end }}
---
{{- $pvcexists := lookup "v1" "PersistentVolumeClaim" .Release.Namespace "geoquery-requests" }}
{{- if not $pvcexists }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: geoquery-requests
  annotations:
    "helm.sh/resource-policy": keep
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.volumes.requests.size }}
  volumeName: {{ $pvname }}
  storageClassName: {{ .Values.volumes.requests.storageClassName }}
{{- end }}
