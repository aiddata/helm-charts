{{- if .Values.mcp.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: geoquery-mcp
  namespace: {{ .Release.Namespace }}
  labels:
    app: geoquery
spec:
  type: ClusterIP
  selector:
    app: geoquery-mcp
  ports:
    - name: http
      port: 80
      targetPort: 8001
{{- end }}
